"use strict"

{
    let addBtn = document.querySelector(".btnPrimary[0]");
    let removeBtn = document.querySelector(".btnPrimary[1]");
    let title;
    let content;

    class NoteTemplate {

        constructor(title, content) {
            this.title = title;
            this.content = content;
        }
    }

    let addNote = (title, content) => {

    }
    let removeNote = (title, content) => {

    }

    addBtn.addEventListener('click', (evenet) => {

    })
    removeBtn.addEventListener('click', (event) => {

    })


}